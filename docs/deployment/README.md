# Deployment

Docker Compose supports local dependencies. Production deployment must supply managed secrets, TLS, persistent PostgreSQL, Redis durability policy, observability, backups, and least-privilege identity.

